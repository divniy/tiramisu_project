<%= simple_form_for @account, :html => {:class => 'form-horizontal'} do |f| %>
  <%= f.error_notification %>

    <%= f.input :name %>
    <%= f.input :role, :collection => @account.class.roles, :required => true, :include_blank => false %>
    <%= f.input :state_event, :collection => @account.state_events, :required => true, :include_blank => false %>

  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to 'Back', :back, :class => 'btn' %>
  </div>
<% end %>
